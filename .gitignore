.vagrant/
.DS_Store

# Presentation sources — public은 PDF만 공개 (편집본 노출 금지, *.key는 아래 키 섹션이 커버)
*.pptx
*.ppt

# Secrets & Environment
.env
.env.*
*.secret

# Certificates & Keys
*.pem
*.key
*.crt
*.p12
*.pfx

# Kubernetes
kubeconfig
*-kubeconfig
*.kubeconfig

# Cloud Credentials
credentials
credentials.json
*.tfstate
*.tfstate.*
.terraform/

# AWS
.aws/credentials
.aws/config
